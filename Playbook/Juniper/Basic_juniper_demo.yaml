---
  -  name: Basic Juniper ansible demo
     hosts: MX 
     gather_facts: no
     tasks:
       -  name: Pulling show version 
          junipernetworks.junos.junos_command:
            commands:
              -  show version
           register: output 

       - name: Display output 
         debug:
           msg: output
                
          
    
  
